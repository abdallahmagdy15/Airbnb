@using Airbnb.Models.PropertySubModels
@model IEnumerable<GuestDetail>
    <h2>All Guest Details</h2>

    <table class="table table-responsive table-hover">
        <thead class="thead-dark">
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Name</th>
                <th scope="col">Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var detail in Model)
            {
                <tr>
                    <th scope="row"> @detail.Id</th>
                    <td> @detail.Name</td>
                    <td><a asp-action="deleteGuestDetails" onclick="return confirm('Are you sure ?')" asp-controller="Admin" asp-route-id=" @detail.Id" class="btn btn-danger">Delete</a></td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td>
                    <button onclick="AddGuestDetail()" class="btn btn-success" id="add-new-amenity">Add new one</button>
                </td>
            </tr>
        </tfoot>
    </table>
